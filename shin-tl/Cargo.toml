[package]
name = "shin-tl"
version = "0.3.0"
edition = "2021"
repository = "https://github.com/DCNick3/shin-translation-tools"
license = "MPL-2"
description = "A tool to help translate strings within shin's SNR files."

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
shin-versions = { path = "../shin-versions", features = ["clap"] }
shin-text = { path = "../shin-text" }

once_cell = "1.18.0"
bumpalo = { workspace = true, features = ["collections"] }
serde = { version = "1.0.193", features = ["derive"] }
csv = "1.3.0"
smallvec = { version = "2.0.0-alpha.1", features = ["write"] }
clap = { workspace = true, features = ["derive"] }
camino = { workspace = true }
